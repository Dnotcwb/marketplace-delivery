[build]
  command = "pnpm install --frozen-lockfile && pnpm turbo build --filter=@marketplace/backoffice"
  publish = "apps/backoffice/.next"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "11"
