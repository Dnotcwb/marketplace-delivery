[build]
  command = "pnpm install --frozen-lockfile && pnpm turbo build --filter=@marketplace/consumidor"
  publish = "apps/consumidor/.next"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "11"
